# Environment variables for ELECTRO PWA
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=electro
DB_USER=electro_user
DB_PASSWORD=electro_pass2024

# JWT Secret (change in production!)
JWT_SECRET=electro-jwt-secret-change-in-production-$(openssl rand -hex 32)
